---
description: Deployment Workflow
---
# Deployment Workflow

1.  Run tests to ensure functionality (`npm run test`).
2.  Build the Next.js application (`npm run build`).
3.  Review build output for any warnings or errors.
4.  Commit changes if all checks pass.

'use client'

// La compra directa de programas está desactivada.
// El acceso se concede exclusivamente por el profesional desde el portal.
export default function MicrohabitosCTA(_props: Record<string, unknown>) {
  return null
}
